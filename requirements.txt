# Core ML libraries
torch>=2.6.0
transformers>=4.51.0
accelerate>=1.2.0
datasets>=3.0.0

# Unsloth for efficient training
# Will be installed in Dockerfile based on CUDA version detection
unsloth-zoo>=2025.1

# TRL for GRPO/PPO/DPO training
trl>=0.12.0

# LoRA and parameter-efficient fine-tuning
peft>=0.14.0
bitsandbytes>=0.44.0

# Tokenizers
tokenizers>=0.21.0
sentencepiece>=0.2.0

# Training utilities
wandb>=0.19.0
tensorboard>=2.18.0
scipy>=1.15.0
scikit-learn>=1.6.0

# Data processing
pandas>=2.2.0
numpy>=1.26.0
tqdm>=4.67.0
pydantic>=2.0.0

# Configuration
pyyaml>=6.0
python-dotenv>=1.0.0

# Optimization
deepspeed>=0.16.0
ninja>=1.11.0
packaging>=23.0
# Flash Attention - will be compiled from source in Dockerfile for compatibility

# Utilities
huggingface-hub>=0.27.0
safetensors>=0.4.0
colorama>=0.4.6
psutil>=6.1.0

# Link Search Agent dependencies
modelscope>=1.32.0  # For downloading models from ModelScope
polars>=0.19.0  # For data processing
psycopg2-binary>=2.9.0  # PostgreSQL adapter for database export
